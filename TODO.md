1. 知识库目录里已经同步完的文档，应当默认是打勾的状态，可以取消打勾。点击开始同步时，没打勾但又实际存在的文档会被自动删掉。等待同步，或者同步中的文档不允许进行打勾或取消打勾的操作
2. 知识库目录里多选框选择和点击文档名字选择两者要同步，点击后的结果要一致。比如点击名字后多选框也要自动勾选。点击多选框同理。
3. 当前从配置页面转向知识库目录页面时界面会卡住，怀疑是等待登录返回时没有一步
4. 需要维护一份元数据记录。记录当前所有已经同步的文档的元数据，包括在飞书里的哈希值，最后一次同步时的版本。以便于判断该文档是否和最新的飞书文档版本一致。