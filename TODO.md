## P1 >
- 修复编译时的 rust warning

## P2 >
- > 修复打开后强制关闭，再次启用时端口被占用的问题。要么自动发现一个未被占用的端口，要么在退出时要自己关掉端口。
- 当知识库内的文档被点击时，会在三种状态之间切换，打勾就自身和子文档全部被勾选。子文档只有不全是打勾状态就是方块状态，切换成方块状态后让所有子文档自己决定自己是否被打勾。当自身取消打勾，所有子文档也取消打勾。

## P3 >
- 需要维护一份元数据记录。记录当前所有已经同步的文档的元数据，包括在飞书里的哈希值，最后一次同步时的版本。以便于判断该文档是否和最新的飞书文档版本一致。