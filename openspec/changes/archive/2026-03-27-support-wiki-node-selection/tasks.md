## 1. Backend Node Classification

- [x] 1.1 Update knowledge-base node classification so library/container nodes are emitted as selectable `folder` scopes instead of unsupported leaf nodes.
- [x] 1.2 Add backend tests covering library-node classification and document discovery from a selected library root.

## 2. Frontend Selection Flow

- [x] 2.1 Ensure library nodes rendered as folders participate in tree selection, source normalization, and task creation without introducing a new scope kind.
- [x] 2.2 Add frontend regression coverage for selecting a library node and showing the resulting sync summary.
