## 1. Verify cleanup-only scope

- [x] 1.1 Confirm the directory-opening error is already fixed by comparing current code, archived OpenSpec evidence, and existing `DONE.md` history.
- [x] 1.2 Remove the stale active entry from `TODO.md` while keeping the remaining queue order intact.

## 2. Document and finalize the cleanup

- [x] 2.1 Add a minimal OpenSpec workflow spec for clearing already-completed TODO items without duplicating `DONE.md` history.
- [x] 2.2 Validate the cleanup-only change artifacts and prepare the branch for archive.
