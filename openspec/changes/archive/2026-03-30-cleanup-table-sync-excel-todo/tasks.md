## 1. Verify cleanup-only scope

- [x] 1.1 Confirm that table sync already exports `sheet` and `bitable` content as `.xlsx` by checking current code and archived OpenSpec evidence.
- [x] 1.2 Remove the stale active TODO entry and promote the next remaining task without touching unrelated queue items.

## 2. Record and finalize the cleanup

- [x] 2.1 Add the queue-specific `DONE.md` confirmation and update the `task-queue-hygiene` spec for first-time cleanup verification.
- [x] 2.2 Validate the cleanup-only change artifacts, then prepare the branch for archive and merge.
