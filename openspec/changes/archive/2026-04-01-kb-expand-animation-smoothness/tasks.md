## 1. Implementation

- [x] 1.1 Wrap `setLoadedSpaceTrees` in `onLoadTreeChildren` with React `startTransition` after `listKnowledgeBaseNodes` resolves (`App.tsx`).
- [x] 1.2 Run `npm run typecheck` and `npm test`; fix any regressions.

## 2. Validation

- [x] 2.1 Run `openspec validate kb-expand-animation-smoothness --type change`.
