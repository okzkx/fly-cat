## 1. Implementation

- [x] 1.1 Add URL normalization when extracting `text_run` link URLs in `src-tauri/src/mcp.rs`
- [x] 1.2 Add a unit test covering percent-encoded `https` links and unchanged canonical URLs

## 2. Verification

- [x] 2.1 Run `cargo test` for the Tauri library / `mcp` tests affected by the change
