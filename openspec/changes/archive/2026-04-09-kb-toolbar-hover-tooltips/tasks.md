## 1. Implementation

- [x] 1.1 Add `Tooltip` (and disabled-button wrapper spans where needed) for home card **全部刷新**, **强制更新**, **批量删除**, **开始同步** in `HomePage.tsx`.
- [x] 1.2 Add `Tooltip` for tree row actions in `KnowledgeTreeNodeTitle` (**重新同步**, **在浏览器打开**, **使用默认应用打开** for document/bitable/folder) with disabled-friendly wrapping for **重新同步** when disabled.

## 2. Validation

- [x] 2.1 Run `npm run typecheck`.
- [x] 2.2 Run `npm test`.
