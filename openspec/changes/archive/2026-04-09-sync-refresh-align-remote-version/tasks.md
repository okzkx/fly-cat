## Tasks

- [x] Update `handleBulkFreshnessAction("refresh")` in `src/components/HomePage.tsx` to refresh + `alignDocumentSyncVersions(..., true)` only; remove strip/task enqueue from the refresh path; adjust user-facing success copy.
- [x] Validate OpenSpec change and run `npm run typecheck` and `npm test`.
