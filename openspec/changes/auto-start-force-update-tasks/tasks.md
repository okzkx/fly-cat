## 1. Spec And Flow Alignment

- [ ] 1.1 Add an OpenSpec delta for `knowledge-tree-display` that requires force-update replacement tasks to auto-start without a manual task-list action.
- [ ] 1.2 Update the home-page task wiring so ordinary task creation and force-update follow-up use the intended automatic start paths.

## 2. Validation

- [ ] 2.1 Run focused validation for the updated task start flow and record the results.
- [ ] 2.2 Archive the change with a Chinese execution report after validation passes.
