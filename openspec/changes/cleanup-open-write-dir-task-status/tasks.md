## 1. Verify cleanup-only scope

- [ ] 1.1 Confirm the directory-opening error is already fixed by comparing current code, archived OpenSpec evidence, and existing `DONE.md` history.
- [ ] 1.2 Remove the stale active entry from `TODO.md` while keeping the remaining queue order intact.

## 2. Document and finalize the cleanup

- [ ] 2.1 Add a minimal OpenSpec workflow spec for clearing already-completed TODO items without duplicating `DONE.md` history.
- [ ] 2.2 Validate, archive, and merge the cleanup-only change back to the base branch.
