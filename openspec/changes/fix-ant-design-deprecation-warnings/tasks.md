## 1. Implementation

- [x] 1.1 Replace every `Space` `direction=` usage with `orientation=` (preserve `vertical` / `horizontal` values).
- [x] 1.2 Replace every `Spin` `tip=` usage with `description=`.
- [x] 1.3 Run focused validation (TypeScript check or project lint script as appropriate).
