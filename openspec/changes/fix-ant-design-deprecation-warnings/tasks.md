## 1. Implementation

- [ ] 1.1 Replace every `Space` `direction=` usage with `orientation=` (preserve `vertical` / `horizontal` values).
- [ ] 1.2 Replace every `Spin` `tip=` usage with `description=`.
- [ ] 1.3 Run focused validation (TypeScript check or project lint script as appropriate).
