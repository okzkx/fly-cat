## 1. Implementation

- [ ] 1.1 Add URL normalization when extracting `text_run` link URLs in `src-tauri/src/mcp.rs`
- [ ] 1.2 Add a unit test covering percent-encoded `https` links and unchanged canonical URLs

## 2. Verification

- [ ] 2.1 Run `cargo test` for the Tauri library / `mcp` tests affected by the change
