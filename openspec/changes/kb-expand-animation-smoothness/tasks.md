## 1. Implementation

- [ ] 1.1 Wrap `setLoadedSpaceTrees` in `onLoadTreeChildren` with React `startTransition` after `listKnowledgeBaseNodes` resolves (`App.tsx`).
- [ ] 1.2 Run `npm run typecheck` and `npm test`; fix any regressions.

## 2. Validation

- [ ] 2.1 Run `openspec validate --change kb-expand-animation-smoothness`.
