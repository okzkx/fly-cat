## 1. Layout

- [x] 1.1 Stack the home sync Card header so `extra` actions render below the title row (`HomePage.tsx`).
- [x] 1.2 Stack the task list Card header the same way (`TaskListPage.tsx`).

## 2. Verification

- [x] 2.1 Run frontend build (or equivalent) to ensure TypeScript and bundling succeed.
