## 1. Layout

- [ ] 1.1 Stack the home sync Card header so `extra` actions render below the title row (`HomePage.tsx`).
- [ ] 1.2 Stack the task list Card header the same way (`TaskListPage.tsx`).

## 2. Verification

- [ ] 2.1 Run frontend build (or equivalent) to ensure TypeScript and bundling succeed.
