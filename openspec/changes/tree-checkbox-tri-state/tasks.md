## 1. Tree Component Configuration

- [x] 1.1 Remove `checkStrictly` prop from Tree component in `src/components/HomePage.tsx`
- [x] 1.2 Keep `checkedKeys` in object format `{ checked: [...], halfChecked: [] }` - leave `halfChecked` as empty array (Ant Design will auto-fill)

## 2. Testing

- [x] 2.1 Verify parent nodes show half-checked (minus) state when partial children are selected
- [x] 2.2 Verify parent nodes show fully checked state when all children are selected
- [x] 2.3 Verify existing checkbox selection behavior still works correctly
